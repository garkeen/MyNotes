## 等式规范/代数规范

抽象操作的符合应该是什么样子

比如stack

pop(push(x,s))=s

peek(push(x,s))=x



比如加法

add(a,add(b,c))=add(add(a,b),c)



如何寻找这些等式？

把方法分为生成器generator，操作器manipulator，和查询器query

查询器*生成器

操作器*生成器



这其实是一个非常巧妙的想法，可以这样想，任何数值都不是凭空来的，都是从一个初始值然后一步步操作来的

因此任何值都可以写成初始值+一系列的生成器符合

比如6=add(1,add(2,3))