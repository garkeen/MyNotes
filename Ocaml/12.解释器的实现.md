## 解释器

词法检查

语法检查

类型检查

如果大部分情况下没有注解，类型推导 Hindley-Milner算法

env |-> e:t <-| Constraints

求值

lexer -> parser -> type checker -> eval ->string of val



lexer -> parser -> type checker被称为前端



## 编译器

lexer -> parser -> type checker -> IR -> Optimizer → CodeGen