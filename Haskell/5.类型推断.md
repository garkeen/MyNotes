## 等式推理

本质上是一种基于类型等式的推理

 比如f x y = (x+y)*2

可以根据+::int->int->int知道x y 必须具有int类型

根据 *:: int->int->int 知道f的输出也是int类型

比如f=map g=map

map2d = map.map

f::(a->b)->([a]->[b])

g::(c->d)->([c]->[d])

可以知道map2d的输入一定是a->b

而([a]->[b])=(c->d) 所以c=[a] d=[b]

所以map2d的输出是[[a]] -> [[b]]