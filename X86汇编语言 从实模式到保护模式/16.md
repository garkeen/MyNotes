## 汇编指令的格式和操作尺寸

操作尺寸：操作数的长度和有效地址（偏移地址）的长度



16位模式 操作数可以是8/16位 有效地址必须是16位

从汇编语言中，操作数的大小是从名字看出来的 al ah是8位 ax是16位

mov al,bh

mov ax,[0x77]



32位模式 内存操作数的长度依附于寄存器操作数的长度

mov al,[eax]

mov ax,[ebx]



Unreal mode 高级实模式

在实模式下使用保护模式的段描述符，从而突破实模式64KB的段限，访问全部4GB内存