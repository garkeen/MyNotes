# 栈

为了维护栈（本质上就是内存的连续数据结构），必须知道栈顶地址，也被分为栈顶段地址SS和栈顶偏移地址SP，即SS:SP是当前栈顶位置

pop 用来输出栈顶数据到某个地方 如果是内存地址 必须给出大小 然后SP+

push 用来压栈 如果是内存地址 必须给出大小 然后SP-

请注意，8086CPU的栈是从高位地址增长到低位地址的！看SP移动的方向是不是和你想象的不一样

为什么？因为此时的SS可以看成栈的顶部，而栈的大小只取决于SP的寻址空间，注意SP是按模来加减的