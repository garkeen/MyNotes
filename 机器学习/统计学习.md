参考：斯坦福大学 统计学习导论；

监督学习和无监督学习

给定[x1 x2 ... xn -> y] 求 [c1 c2 .. cn -> ?]

对任意点 求E(y|X1=x1,X2=x2,...) 得到estimate Y = f([xn])

但很可惜，不是每个点都有数据，或者数据极少

最近邻方法

固定窗口大小（但依然可能没有任何数据）

固定邻居取样（扩展窗口直到获取到10%的样本）

核估计







非参数化模型与参数化模型

假设f具有一定的数学函数结构，而且这个结构由有限的固定多的参数决定



预测与实际

f_predict=f_estimate

f_real=f_ideal+noise

error=f_real-f_predict=(f_ideal+noise)-f_predict

E(noise)=0

训练误差与泛化误差





分类器

可以认为是一种回归 但是输出必须是多个类别的一个 所以要么函数就是离散的 要么是连续的 然后通过选择变成离散的 比如每次选择概率最大的那一个（贝叶斯分类）



量化

有些指标是可以量化的 比如年龄 余额 身高 体重 这些就是所谓的定量指标

有些指标量化起来比较麻烦 比如性别（男/女） 颜色（红 黄 蓝） 种族（亚洲人 北美人 印度人），我的意思是，如果红是1 蓝是2 那么会出现红<蓝这个性质 也就是添加到了不应该具有的序结构 这些似乎不应该量化的指标叫做定性指标



线性回归

y=ax1+bx2+cx3

y=ax1+bx2+cx2x3

y=ax1+blog(x2x3)

一般的，线性模型指的是模型中每个未知参数都单独作为一次项出现，也就是sigma 未知参数*f(x1,x2,...)的形式



逻辑斯蒂回归

把线性回归y的输出放到[0,1]区间 那么

y`= 1/(1+e^-y) 或者 y=ln(y'/1-y') 

比如a+bx=ln[ p(Y=1|X)/1-p(Y=1|X) ]

问题来了，我们如何训练？由于样本值是0和1，而且ln(0/1-0)=ln0=-∞ ln(1/1-1)=ln∞=∞ 最小二乘法是不可能的

使用最大似然估计

如果是二类 那么使用sigmoid 如果是多类 那么使用softmax

使用梯度下降，由于MLE是一个凸函数，梯度下降可以保证收敛到最优



判别分析

假设不同类的数据服从某种分布（比如正太分布），然后计算在X=sample的情况下sample属于分布A的概率，概率最大的就是sample对应的类别。

注意，我们讨论的是在哪种分布下出现这个sample的概率最大，即X=sample的情况下sample属于分布A的概率，而不是在分布A下X=sample的概率，然后取概率值最大的分布。为什么？因为样本是等概率出现的（也就是我们使用的样本数据默认都是等概率的），但是类别不是，每个类别出现的整体概率可能不相同。

使用P(X=...|Y=...)比较做决策最大其实暗含了假设所有的Y等概率出现，但是这是不对的，最简单的例子是假设有100只猫和900只狗，每个个体等概率出现，但是每个类别不是等概率出现，所以我们需要用P(Y=...|X=...)做比较，而计算这个式子就是使用所谓的贝叶斯定理，转换为计算P(X=...|Y=...),因为后者才是分布告诉你的值，而前者无法直接得到。

为什么最大似然估计不用考虑？因为最大似然把所有样本同等考虑的相乘，本质上就相当于样本是等概率出现的，而类别比例体现在乘积中有多少样本是这个类别的比例，显然类别占比越大，乘积项就越多，为了最大似然，这个类的系数会被提的更高。



非线性回归







算法实现

最近邻

打表？需要空间太多 因此采用即时计算 也就是给一个点 动态计算出函数的结果



